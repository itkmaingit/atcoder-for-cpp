#include <iostream>
#include <vector>
using namespace std;

void reverse_loop()
{
  vector<int> vec;
  for (std::size_t i = 0; i < vec.size(); ++i)
  {
    const auto irev = vec.size() - 1 - i;
    /* TODO */
  }
}